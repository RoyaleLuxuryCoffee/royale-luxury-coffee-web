---
description: Image assets — where they live and the risk of broken references.
globs:
  - "*.html"
  - "imagenes/**"
alwaysApply: false
---

## Images

All images live in `/imagenes/`. Several files referenced in HTML (e.g. `bag.png`) may not exist on disk — always verify a file exists before editing image references.
