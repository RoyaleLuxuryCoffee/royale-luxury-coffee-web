---
description: Product catalog structure in server.js — how products and prices are defined.
globs:
  - "server.js"
alwaysApply: false
---

## Product catalog

Products are hard-coded in `server.js` in the `products` array. `priceCOP` is the price in Colombian pesos (integer, no decimals).
